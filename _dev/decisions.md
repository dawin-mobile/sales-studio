# decisions.md — 設計・方針判断の記録

<!-- 形式:
## YYYY-MM-DD: [何についての判断か]
- 選択肢: A / B / …
- 決定: 採用したもの
- 理由: なぜそうしたか
-->

---

## 2024〜2025: データソースを Google Sheets 直読みにする
- 選択肢: A) Sheets API 直読み / B) GAS で DB に同期してから DB 読み
- 決定: A（Sheets 直読みをメイン）
- 理由: スプレッドシートが既存の業務基盤であり移行コストがない。少人数（〜30人）かつ更新頻度が低いデータにはDBのオーバーヘッドが不要。Vercel の `revalidate=300`（5分キャッシュ）で Sheets API のクォータ制限を回避しつつ十分な即時性を確保できる。

---

## 2024〜2025: Neon DB の用途を「書き込みが必要なデータのみ」に限定する
- 選択肢: A) 全データを DB に移行 / B) Sheets をメインにして DB は書き込み系のみ
- 決定: B
- 理由: 読み取り専用の実績データは Sheets 直読みで十分。DB はシフト・日報・評価など GAS から Webhook で定期同期されるデータのみに絞り、Neon の無料枠（compute hours）を節約する。

---

## 2024〜2025: 自動同期を GAS（Google Apps Script）で行う
- 選択肢: A) GAS Webhook / B) 外部 cron サービス / C) Vercel Cron
- 決定: A（GAS）
- 理由: スプレッドシートと同じ Google エコシステム内で完結する。スプレッドシートの onEdit トリガーや時間トリガーが使えるため、外部サービス不要。既存の業務フロー（スタッフがフォームで日報提出 → Sheets 自動更新 → GAS が Webhook 送信）に自然に組み込める。

---

## 2024〜2025: 認証に NextAuth.js（credentials プロバイダー）を使う
- 選択肢: A) Google OAuth / B) 独自 credentials / C) Clerk など SaaS
- 決定: B（独自 credentials：ユーザID ＋ ハッシュパスワード）
- 理由: スタッフ全員が Google アカウントを持っていない。外部 SaaS は費用と依存リスクがある。スタッフ情報シートでユーザ管理できる方が運用が簡単。パスワードは bcrypt でハッシュ化して Sheets に保存。

---

## 2024〜2025: ホスティングを Vercel にする
- 選択肢: A) Vercel / B) AWS / C) GCP / D) Cloudflare Pages
- 決定: A（Vercel）
- 理由: Next.js との親和性が最高で設定がほぼ不要。Hobby プランで無料運用可能（〜30人の社内ツール規模）。main ブランチへの push で自動デプロイされる CI/CD が最短で構築できる。

---

## 2025: PWA 対応（インストール可能な Web アプリ）にする
- 選択肢: A) ネイティブアプリ / B) PWA / C) Web のみ
- 決定: B（PWA）
- 理由: スタッフがスマホのホーム画面に追加して使う想定。ネイティブアプリはストア申請・配布が煩雑。Web のみだと毎回 URL を開く手間がある。PWA なら Web の管理コストでアプリ体験が提供できる。

---

## 2025: secretMode（裏メニュー）でロール制限付き機能を隠す
- 選択肢: A) ロールごとにメニューを切り替え / B) 隠しジェスチャーで表示
- 決定: B（ロゴを3回タップで secretMode トグル）
- 理由: 育成管理・遅刻早退など管理者専用の機能を通常メニューに混在させるとスタッフが混乱する。隠しアクセスにすることで UI をシンプルに保ちつつ管理者だけが使える。

---

## 2025: キャッシュ戦略を Vercel の revalidate=300（5分）にする
- 選択肢: A) キャッシュなし（毎回 Sheets 読み） / B) 5分キャッシュ / C) ISR（オンデマンド再検証）
- 決定: B（5分キャッシュ）
- 理由: Sheets API の無料クォータは 300req/min。30人が同時アクセスしても同一 URL はキャッシュされるため実質 1req/5min に抑えられる。日報データは 5分の遅延が許容範囲（リアルタイム性よりクォータ安全性を優先）。

---

## 2026-06: 月別データのフォールバック（データなし時に先月を自動表示）
- 選択肢: A) コールバック方式（AttendanceTable → page.tsx へ通知して月変更） / B) fetchData 内ループ方式
- 決定: B（fetchData 内でループして対象スタッフのデータがある月を探す）
- 理由: A のコールバック方式は「データなし検知 → 月変更 → 再 fetch → データなし → 月変更」が高速で繰り返され、Google Sheets API のレート制限（Read requests per minute）を超過するバグが発生した。B は fetchData の中で最大12回の順次リクエストに制限されるため安全。
- 補足: 橋本章平（現場に出ないマネージャー）は NO_FALLBACK_USERS リストで除外。

---

## 2026-06-05: 担当タブのデータ取得ロジック
- 選択肢: A) 独自実装（スタッフ情報を主軸に知識シートを引く）/ B) 社員連絡先APIと同じロジックを再利用
- 決定: B（社員連絡先と同じ `startsWith` 照合）
- 理由: 社員連絡先（`/api/contacts`）がすでに「知識シートB列の担当姓 → スタッフ情報T列フルネームをstartWithsで照合」するロジックを確立済み。同じ問題（名前形式の不一致）を解決している実績があるため再利用した

---

## 2026-06-05: GAS管理をclaspでローカル化・フォルダ分離
- 選択肢: A) GASエディタで直接編集 / B) claspでローカル編集→push
- 決定: B（clasp）
- 理由: Claude Codeからそのまま編集・反映できる。git管理もできる。
- 構成:
  - `gas/sync-nippo/` → 日報合算GAS（スクリプトID: 1Tq50lWRhcUmWPjcJ7agsGCviOfucQ49OBV1M92EfRWBYB5L5d43y37yH）
  - `gas/sync-shift/` → 日報催促GAS（スクリプトID: 1oCZzurhVbvEHPdRZTQa1HEt34EcRXxcEU4JOGTKn5Ap9KhwQah_UlZAd）
- 注意: sync-shift に `コード.gs`・`ColorManager.gs` が上司追加済み。絶対に触らない。
- claspログインアカウント: dawinmobilebot@gmail.com
- push手順:
  - sync-nippo変更時 → `cd gas/sync-nippo && clasp push`
  - sync-shift変更時 → `cd gas/sync-shift && clasp push`

---

## 2026-06-05: Talknote実績報告をトークノート受信録と分離
- 選択肢: A) 全メールをトークノート受信録に統合 / B) 実績報告は専用シートに分離
- 決定: B（実績受信録シートに追記、DBへは送らない）
- 理由: 実績報告は長期蓄積が目的でDB同期不要。通常メッセージと混在させると管理しづらい。
- 実装: 件名の内容でシートへ振り分け。過去データ取込は各専用関数で実行。DBへは送らない。
  | 件名キーワード | 保存シート | 取込関数 |
  |---|---|---|
  | `実績報告` + `ノートに投稿しました` | 実績受信録 | `fetchJissekiEmails_PastData()` |
  | `☆関東社員☆` + `ノートに投稿しました` | 終了報告受信録 | `fetchShuryoEmails_PastData()` |
  | それ以外 | トークノート受信録 | — （DB同期あり） |

---

## 2026-06-08: GAS sync-nippo を機能ごとに複数ファイルへ分割
- 選択肢: A) 1ファイルに全機能をまとめる（目次コメント方式）/ B) 機能ごとにファイルを分ける
- 決定: B（9ファイルに分割）
- 理由: 元の sync-nippo.gs が1467行になり目的のコードを探しにくかった。GASは同プロジェクト内の複数 `.gs` ファイルをすべて同じグローバルスコープで実行するため、import不要で分割できる。
- 分割後の構成（`gas/sync-nippo/` 以下）:
  | ファイル | 内容 |
  |---|---|
  | `config.gs` | 設定値・定数・共通関数（CONFIG/DAWIN/STAFF_SYNC等） |
  | `sync-db.gs` | DB同期（syncAll・syncNippoSheet等）、1時間おき自動実行 |
  | `talknote.gs` | Talknoteメール取得・シート記録・DB同期、15分おき自動実行 |
  | `notifications.gs` | シフト通知（18時）・日報催促（21時）・日報合算 |
  | `sync-staff.gs` | スタッフ情報の転記・DB反映、6時自動実行 |
  | `sync-eval.gs` | 育成管理データ（スキル評価＋知識チェック）のDB送信、手動実行 |
  | `helpers.gs` | 内部ヘルパー関数・onEditTrigger |
  | `triggers.gs` | トリガー管理（setupTriggers/clearTriggers） |
  | `calendar.gs` | Googleカレンダーへのシフト登録、4時自動実行 |
- 注意: グローバル定数（CONFIG・DAWIN等）は `config.gs` にのみ定義。他ファイルに同名の `const` を書くと「すでに宣言済み」エラーで全体が動かなくなる。
- あわせて対処: `ジョブカン周知.js` に `send-staff-id.gs` と同名の関数（sendStaffIds・dryRun・getTargets_）があり重複警告が出ていたため、`ジョブカン周知.js` 側の関数名を `sendJobcanInfo`・`dryRunJobcan`・`getJobcanTargets_` にリネームした。
- トリガー間隔: fetchAndSyncTalknote=15分おき、syncAll=1時間おき（他は変更なし）

---

## 2026-06-09: sync-shift の定期同期を 10分おき → 1時間おきに変更
- 選択肢: A) 10分おき（現状） / B) 1時間おき
- 決定: B（1時間おき）
- 理由: シフト編集時は onEditTrigger が即時 Neon 同期するため、定期実行は保険にすぎない。10分おきだと Neon のオートスリープ（5分）が機能せず常時稼働になり compute hours を無駄消費していた。
- 対応: `gas/sync-shift/sync-shift.gs` の `everyMinutes(10)` → `everyHours(1)` に変更、clasp push 後に GAS エディタで `setupTriggers()` を再実行して反映済み。
- 補足: GAS トリガーのオーナーは「setupTriggers() を実行したアカウント」になる。オーナーを変えたい場合は、旧アカウントで `clearTriggers()` → 新アカウントで `setupTriggers()` を実行すれば良い。他アカウントのトリガーは自分では削除できない点に注意。

---

## 2026-06-09: 稼働カードに実績報告タブを追加
- 選択肢: A) Talknoteタブ内に追記表示 / B) タブ切り替え / C) 別セクション
- 決定: B（タブ切り替え）
- 理由: 既存のTalknote表示を崩さずシンプルに追加できる。UI一貫性が高い。
- 実装:
  - `src/app/api/jisseki/route.ts` 新規作成（実績受信録シートを現場別に整理して返す）
  - `TalknoteCard.tsx` にタブ state と `/api/jisseki` へのフェッチを追加
  - バッジ（MNP/新規）はTalknoteの `data.siteMap` から計算して流用（実績報告テキストを解析しない）
- 現場紐づけ: 実績受信録に現場名はないため、送信者名とシフトシートのスタッフ名をスペース除去＋前方一致で照合

---

## 2026-07-01: シフトビューの月表示をダッシュボードの自動遡りから独立させる
- 選択肢: A) selectedMonthを共有（従来）/ B) shiftMonth を別stateとして持つ
- 決定: B（shiftMonth を独立したstateに分離）
- 理由: ダッシュボードのautoFallback（当月データがない場合に自動で過去月を探す）が走ると、シフトビューも連動して過去月に巻き戻る問題があった。シフトは未来月も確認したいため、月ナビはシフト専用stateで管理する
- あわせてダッシュボードのautoFallback自体も廃止（fetchDataから最大12ヶ月遡るループを削除）

---

## 2026-07-01: 社員連絡先APIをW列ロールで自動フィルタ
- 選択肢: A) 個別に連絡先フラグを立てる / B) W列「社員」「管理者」で自動判定
- 決定: B（W列ロールで判定）
- 理由: スプレッドシートにスタッフを追加するだけで自動的に連絡先に載るようにしたかった。個別フラグだと追加時に設定漏れが起きる
- 実装: `/api/contacts` でW列 `role` が「社員」または「管理者」かどうかを確認してフィルタ

---

## 2026-07-01: AccessLog UIをシングルカラー（白）に変更
- 選択肢: A) タブ別カラー（カラフル）/ B) 白一色（BAR_COLOR）
- 決定: B（白一色: `rgba(255,255,255,0.75)`）
- 理由: カラフルなUIが見づらいとの指摘。白一色にすることで他のUIパーツと統一感が出る

---

## 2026-07-01: 旧サイト（sales-studio-iota.vercel.app）の移行通知実装
- 選択肢: A) リダイレクト / B) 移行通知ページを旧サイト上に表示
- 決定: B（通知ページを旧サイトに追加）
- 理由: 旧サイトはGitHubとVercelの接続が切れているため自動デプロイ不可。手動で `old-site-notice` ブランチを作成（2026-05-07のコミット `8f4f2e6` を起点）し、Vercel CLI（グローバルインストール `v50.18.0`）で1回だけデプロイ
- 対象: ロール「管理者」「社員」のみ告知画面を表示。アルバイトは従来通り使用可能
- バイパス: 開発確認用に `dawin0914` を入力するとlocalStorageに保存され、以降は表示されない
- 注意: localStorageは同ブラウザ・同デバイスで永続するため、コードを知っているユーザーは告知を永久にスキップできる。締め出したい場合はコード変更または告知削除して再デプロイが必要
- Vercel CLI認証: グローバル `vercel`（volta管理）で `shostar6411-2288` 個人アカウント認証済み

---

## 2026-06: 合算データの mergeSheets_Complete を全削除→上書きにする
- 選択肢: A) 全削除→上書き / B) 追記方式（重複チェックあり）
- 決定: A（全削除→上書き）
- 理由: 追記方式は GAS の定期実行（10分おき）で重複データが蓄積される。フォームの誤入力訂正も全書き直しで自動反映される。ただし FormSheetA のグループ化（折りたたみ）でデータが消える問題あり（→ 経理スタッフに折りたたみ運用要相談）。

---

## 2026-07-10: 稼働カードの折りたたみUI設計
- 選択肢: A) 別途「すべて開く/閉じる」ボタン2つ / B) タブ行右端に単一アイコントグル（A+Bの合体）
- 決定: B（単一アイコントグル）
- 理由: ボタン2つはUIが野暮ったい。タブ行右端のアイコントグル1つで全開閉を切り替える方がすっきりする
- 実装: SiteCardはデフォルト閉じた状態（useState(true)）。ヘッダークリックで個別開閉。親からexternalCollapsedプロップで一括制御。アイコンは全閉時に展開矢印、全開時に折りたたみ矢印に切り替わるSVG

---

## 2026-07-10: Talknoteノート振り分けをホワイトリスト方式に変更
- 選択肢: A) 除外リスト方式（関東社員・実績報告を除く全部を通す）/ B) ホワイトリスト方式（全社だけ通す）
- 決定: B（ホワイトリスト）
- 理由: 除外リスト方式は新しいノートが追加されたとき自動的に混入するリスクがある。全社だけ明示的に通す方が安全
- 実装: `subject.includes('ノートに投稿しました') && !subject.includes('「全社」')` の場合スキップ

---

## 2026-07-31: 稼働カードに「実績報告」自動生成ボタンを追加
- 背景: スタッフはTalknoteの雑談投稿（実績）を見ながら、正式な「実績報告」フォーマットを毎回手打ちしている。手間を減らしたい。
- 選択肢: A) 雑投稿を集計してテンプレートを完全新規生成 / B) 前日の実績報告カードをベースに当日実績を足し算して生成
- 決定: B
- 理由: 「店舗込累計」「ブース累計」（イベント期間中の累計）はアプリ内データだけでは算出不可能（他ブース分含む等、外部要因あり）。前日の実際の投稿内容を土台にする方が実態に近い
- 実装: `src/components/TalknoteCard.tsx`
  - Talknoteタブの各現場カードに「生成」ボタンを追加（コピーボタンの隣、実績報告タブには出さない）
  - 押下時に`/api/jisseki?date=前日`を追加取得し、前日の実績報告テキストを正規表現でセクション分割（`splitJissekiSections`）して「催事場所・紐付け店舗・集客装置・ブース目標・店舗込累計・ブース累計」を読み取る
  - 当日分はTalknote雑投稿から`parseJissekiBreakdown`で新規集計（新規/MNP × au/UQ × 端末/SIM単、O19、MNP即決）
  - 累計は「前日の値＋当日のブース実績」の単純加算（他ブース分などのズレはスタッフが手直しする前提）
  - 生成テキストをクリップボードにコピーするのみ（Talknoteへの投稿は行わない）
- 集計ロジックの業務ルール（要件ヒアリングで確定、パーサ実装の前提）:
  - 新規HS＝新規＋MNPの合算値（MNPは内数、別枠加算しない）
  - au/UQ判定：「元キャリア→au/UQ」の矢印表記優先、なければ本文中のau/UQ表記の先勝ち。元キャリア（SB＝SoftBank、Y/ワイモバ/ワイモバイル/YM/Yモバ＝Y!mobile等）は判定に使うだけで出力には使わない
  - 端末/SIM単：au/UQ判定した本文中の「sim」「端末」表記で判定
  - MNP即決：「戻り」というワードがMNP表記より前にあれば即決ではない、それ以外はデフォルトで即決扱い
  - 上記は自由記述からの推測のため完全一致は保証されない。個人実績欄に生テキストもそのまま貼るので、スタッフが目視確認・手直しできる前提で設計
- 未検証事項: ローカルでの型チェック・自作の単体テスト（Node直書きスクリプト）は通したが、認証が必要なため実ブラウザでのUI動作確認はできていない。翔に実機確認を依頼

---

## 2026-07-10: 上司の Report Studio（終了報告の自動採点）を sales studio に統合する方針
- 背景: 上司が個人で作った Report Studio が、Talknoteの終了報告テキストを Claude/Gemini で判定し「育成」「現場指揮」を自動採点・可視化している。データソースは sales studio と同じスプレッドシート（終了報告受信録シート）。sales studio 側では終了報告受信録シートは現状表示に使われていない
- 選択肢（コード入手）: A) GitHub招待 / B) zip等でコードのみもらう / C) 仕様だけ聞いて作り直す
- 決定: A（上司のGitHubリポジトリにコラボレーターとして招待してもらい、コードを直接移植する）
- 選択肢（APIキー）: A) sales studio用に新規キー発行 / B) 上司の既存キーを流用
- 決定: B（上司の既存キーをそのまま使う。本人了承済み）
- 選択肢（配置場所）: A) 終了報告受信録タブを拡張 / B) 新規独立タブ・ページ
- 決定: B寄り（secretMode裏メニューに新規タブとして追加。社員のみ閲覧可）
- 理由: 終了報告受信録は現状sales studioで死んでいる（未使用の）データ経路なので、新しいタブとして育成・現場指揮の採点機能をそのまま持ち込むのが自然。裏メニューにするのは育成評価という管理寄りの情報のため（既存のsecretMode方針 [[secretMode]] と同じ考え方）
- 未確定: 「社員のみ」を既存のsecretModeジェスチャー（誰でも知っていれば入れる）だけで担保するか、ロール（W列）でも制御するかは、上司のリポジトリを見てから決める
- 次のアクション: 翔が上司にGitHubリポジトリへのコラボレーター招待を依頼 → 招待が来たらコードを確認して実装計画を立てる

---

## 2026-07-10: GitHub/Vercel/Neonアカウントを個人から会社アカウントへ移行済み
- 選択肢: A) 翔個人アカウントのまま運用 / B) 会社アカウント（dawin-mobile）を新規に作り直して移行
- 決定: B（会社アカウントへ移行）
- 理由: 属人性の排除（翔個人に依存する体制を避けるため）。GitHubは元々 `sho-tarosu`（翔個人）で開発していたが、`dawin-mobile` アカウントを新規作成して移行した
- 補足: 過去のコミット履歴には旧アカウント名（sho-tarosu）が残っているが、現在の正規アカウントは `dawin-mobile`。他サービスとの連携やコラボレーター招待は今後すべて `dawin-mobile` を使う

---

## 2026-07-30: Report Studio統合方針を「コード移植」から「スプレッドシート閲覧」に変更（2026-07-10の決定を更新）
- 背景: 2026-07-10時点では「上司のGitHubリポジトリから採点ロジックをsales studioに移植する」方針だった。しかし上司（橋本さん）から「Report Studioは今まで通りこちらで運用継続。ロジック移植・APIキー発行・Cloud Run版の廃止は不要。sales studio側はその結果を閲覧できる窓口として連携してほしい」という指示があり方針転換
- 調査: 上司のGitHubリポジトリ（`shoheihashimoto-svg/report-studio`、ZIP経由で取得・閲覧）を確認したところ、Report Studioの採点ジョブ（Cloud Run Job、夜間バッチ）は結果を `NIPPO_SS`（日報合算シートID）の `採点結果` タブに書き込んでいた。sales studioが使っている `GOOGLE_SPREADSHEET_ID` のシート一覧を実際に取得し、同じ「採点結果」タブが存在することを確認した → **Report Studioとsales studioは既に同じスプレッドシートを共有している**
- 選択肢: A) スプレッドシート直読み（sales studio側に`採点結果`タブを読むだけの新規表示タブを追加。既存の`getSheetData()`パターン・既存の`auth()`セッション認証をそのまま使う） / B) 採点ロジックごと移植（7/10の元計画、上司が今回不要と明言） / C) 上司にAPI窓口を新規に作ってもらう（APIキーのやり取りが発生し、上司の「APIキー発行不要」という条件と矛盾） / D) Report Studioの画面をiframeで埋め込む（Report Studio自体へのログインが必要になり露出リスクが増えるため却下）
- 決定: A（スプレッドシート直読み）。上司の制約条件（移植不要・APIキー不要・Cloud Run廃止不要）を満たしつつ、最初の懸念（独自URLの露出リスク）がそもそも発生しない唯一の方式
- 閲覧範囲: 「社員以上」で上司と合意済み（アルバイト・業務委託は不可、既存の育成管理と同じ範囲）
- リスク・注意点: ①採点は夜間バッチのためリアルタイムでは反映されない ②Sales Studio側からは閲覧のみで、評価の修正・確定は引き続きReport Studio（上司側）で行う ③「採点結果」タブの列構成（A〜V、22列）はReport Studio側のプログラムが決めているため、上司がロジックを変更する際は事前に一声かけてもらう約束をしておくこと ④採点ジョブへの書き込み権限は上司のGoogle Cloudプロジェクト側で管理されており、sales studio側では関知できない
- 次のアクション: `/api/growth`の認証漏れ修正 → `採点結果`タブを表示する新規タブ・APIルートを実装

---

## 2026-07-31: 実績報告自動生成をau/UQ以外のキャリア（SB/Y）にも対応
- 背景: 現場によって扱うキャリアの組が違う（au/UQ系ブース、SB/Y系ブース）。SB系の現場で生成すると、au/UQの内訳ラベルのまま数値が全部空欄になる不具合が出た
- 決定: 現場名に「SB」が含まれていればSB・Y系ブースと判定し、内訳のラベル（au→SB、UQ→Y）とキャリア判定の対象キーワードを両方切り替える
- 実装: `TalknoteCard.tsx`に`detectCarrierFamily(site)`を追加（現場名に`SB`を含むかで判定）。`detectCarrier`はfamily引数を受け取り、au-uq系は従来通りau/UQ表記、sb-y系はSB/ソフトバンク・Y/Y!モバイル/ワイモバ等の表記を先キャリアとして判定する。表示ラベルは`FAMILY_LABELS`で切り替え
- 未対応: 現状「現場名にSBを含む」以外の判定条件はない。今後SB/Y系でも「SB」の文字列が現場名に入らないケースが出てきたら、判定方法を見直す必要がある

---

## 2026-07-31: SB/Y系ブースは実績報告のテンプレート構造自体がau/UQ系と別物と判明、専用テンプレートを実装
- 背景: 実際のSB南砂の前日報告を見ると、単にラベル（au→SB、UQ→Y）を変えるだけでは足りず、テンプレート構造自体が違った
  - O19の概念がない（O19新規・MNP即決の行が存在しない）
  - 新規HSの内訳が「MNP／新規／アップ（既存客のプラン変更等）」の3分類（au系は新規／MNPの2分類）
  - 固定セクションが「SB光／ソフトバンクAir」（au系は au光／ビックローブ光／JCOM／ホームルーター）
  - LDの電気が「おうち電気」という呼び方（au系は au電気）
  - 目標・累計の見出しが「新規HSアップ含め」（au系は単に「新規HS」）
- 決定: `buildJissekiReport`をfamilyで完全に分岐させ、sb-y系は専用のテンプレート構造で組み立てる（au-uq系の構造を流用しない）
- 実装: `TalknoteCard.tsx`
  - `JissekiBreakdown`に`upTotal`（アップ件数）を追加。`UP_RE = /アップ(?:セル)?[x×]?\s*(\d+)?/gi` で雑投稿から集計
  - `splitJissekiSections`を文字位置ではなく行単位に変更（見出し行の絵文字装飾が欠けないように）
  - `extractNumber`/`extractText`のラベル・コロン間に任意文字を許容（`[^:：\n]*`）：「新規HS アップ含め：」のような補足付きラベルにも対応するため
  - sb-y系の目標・累計の「┗新規」「┗アップ」は、内訳の見出し行「新規HS」に含まれる部分文字列と衝突するため、`┗`を含めたラベルで検索して区別
  - sb-y系の新規HS（全体・累計）は MNP＋新規＋アップ の合算値として都度計算し直す（前日の見出し合計値をそのまま使わない。au系の新規HS計算と同じ考え方）
- 教訓: 1件のサンプルだけで「ラベルを付け替えれば済む」と判断せず、実際のテンプレート全体を見てから設計すべきだった。キャリアの組が違うだけでなく、業務ルールそのもの（O19の有無、アップという概念）が現場タイプによって違う可能性を考慮する

---

## 2026-07-31: ダッシュボードトップ（獲得状況）の読み込みが遅い件を調査（未対応）
- 背景: 実績報告生成機能の作業中に「読み込みが体感2〜3倍遅くなった」と相談があり、原因調査した
- 調査結果: 今回の実績報告生成の変更（`TalknoteCard.tsx`）とは無関係。ダッシュボードトップの`/api/data`（`src/app/api/data/route.ts`）が「合算データ」シートを毎回まるごと読み込んでおり、そのシートが**17,464行**まで育っていて取得だけで約1.9秒かかる（グラフ用データ_年代7,481行・グラフ用データ_家族構成7,797行も同時に読む）。この設計は今回のセッションで変更していない、以前からある挙動
- 対応: 今回は見送り（実績報告機能の作業を優先したため）。翔と合意の上、別タスクとして改めて着手する
- 次のアクション候補（着手時に検討）: 月単位でのA1範囲指定によるシート読み込み絞り込み、キャッシュ導入、DB移行のいずれか。`getSheetData`はA1範囲を指定できる仕様なので、月別に別シートに分けているなら現状でも範囲指定自体は可能なはず（実装未確認）

---

## 2026-07-31: 稼働カードの読み込み高速化（キャッシュ＋遅延ロード＋batchGet）
- 背景: 稼働カード（Talknoteタブ）の「読み込み中...」が長い、という相談。実測して原因を特定した
- 実測結果（ローカルからGoogle Sheets APIを直接叩いて計測）:
  - シフトSS「26年7月【東京】」= 約2.4秒、「26年7月【福岡】」= 約2.3秒。**452行しかないのに遅い**
  - 列を`A:T`に絞って転送セル数を1/8（64,324→7,719セル）にしても約2.2秒しか縮まない → **データ量ではなくシフトスプレッドシート自体がGoogle側で重い**のが原因（メインSSの「実績受信録」は0.3秒）
  - `/api/talknote`と`/api/jisseki`が**それぞれ**東京・福岡を読んでおり、同じシートを合計4回取得していた
  - Sheets APIの読み取り上限は60リクエスト/分/ユーザー。1画面表示で4リクエスト消費 = 15表示/分で頭打ちになる状態だった（検証中に実際に429を踏んだ）
- 選択肢:
  - A: シフトシートをサーバー側でキャッシュ
  - B: 実績報告タブを遅延ロード（開くまで読まない）
  - C: 東京＋福岡を`batchGet`で1リクエストにまとめ、列範囲も絞る
  - D: シフトスプレッドシート自体を軽くする（数式・IMPORTRANGE削減）
- 決定: **A＋B＋C を採用**。Dは本命だがスプレッドシート側の改修が必要なため今回は見送り
- 実装:
  - `src/lib/sheets.ts` に `getShiftMonthData(month)` を新設。月単位で東京(A:T)＋福岡(A:L)を`batchGet`で1リクエスト取得し、**5分間メモリキャッシュ**。さらに「取得中の同一Promiseを共有」する仕組みを入れ、2つのAPIが同時に呼んでもSheetsへのアクセスは1回だけにした
  - `batchGet`はシートが1つでも存在しないと全体が失敗するため、失敗時は個別取得にフォールバックする（月シートが未作成の場合に片方だけでも表示できるように）
  - `/api/talknote`・`/api/jisseki` を `getShiftMonthData` 利用に統一（重複していた`buildSheetName`を削除）
  - `TalknoteCard.tsx`: 実績報告タブは開かれるまで`/api/jisseki`を叩かない。読み込み済みの日付は再取得しない（`useRef`で記録）
  - `/api/talknote` にあった `export const revalidate = 300` は `dynamic = 'force-dynamic'` に打ち消されて無効だったため削除（コメントは「5分キャッシュ」とあったが実際は効いていなかった）
- 理由: 4回 → 5分に1回までアクセスを削減でき、レート制限のリスクも同時に解消できる。キャッシュ5分は「シフト表を編集しても最大5分は反映されない」トレードオフだが、稼働カードは当日のシフト表示用途なので許容と判断（翔と合意）
- 検証: 列を`A:T`/`A:L`に絞った影響を7月の全31日分で新旧比較し、現場・スタッフ・代理店の抽出結果が**完全一致**することを確認。行数は452→450行に減るが、減った行はD列（現場名）が空でもともと除外対象だった

---

## 2026-08-14: 実績報告のSB/au版テンプレート判定にシフトシートC列（キャリア）を追加
- 背景: 2026-07-31時点の判定は「前日報告の催事場所・紐付け店舗に SB / バンク が含まれるか」だけだった（当時から「他の判定条件がない」ことを課題として記録済み）。前日報告がない・表記にSBが出てこない現場ではau版テンプレートで生成されてしまう
- 選択肢: A) 現場名の別名辞書を持つ / B) シフトシートのキャリア列（C列）を使う
- 決定: B。シフト表のC列に「au」「SB」「増強」が必ず入っており、現場の実態と一致する最も確実な情報源
- 実装:
  - `/api/talknote`・`/api/jisseki` の `siteOrder` に `carrier: row[2]`（C列）を追加
  - `TalknoteCard.tsx` の `detectCarrierFamily` を `detectCarrierFamily(shiftCarrier)` に変更。**C列に「SB」が含まれればsb-y、それ以外は全部au-uq**という単純な二択にし、従来の催事場所・紐付け店舗の文字列判定は廃止
  - C列は全角「ＳＢ」で入力されることがあるため、`toHalfWidthAlpha`で英字を半角化してから判定する（大文字小文字も無視）
- 理由: 翔に確認したところ「SB現場はC列が絶対にSBになる」運用とのことなので、C列だけで判定できる。フォールバックを残すと、C列が「増強」の現場で店舗名の文字列から誤判定するリスクの方が大きい

---

## 2026-08-27: 日報未提出アラートの除外ユーザーをAPI側の定数リストで管理
- 背景: 事業部長の橋本（ID: `hashimoto`）は日報を提出しない運用だが、シフトには入るため `/api/nippo-check` が毎回「日報未提出です」を返していた
- 選択肢:
  - A: `NippoAlert.tsx`（フロント）でユーザーIDを見て非表示にする
  - B: `/api/nippo-check` の入口で除外ユーザーを弾く
  - C: スタッフ情報シートに「日報免除」列を追加して運用側で管理する
- 決定: **B**。`NIPPO_ALERT_EXEMPT_USER_IDS` という定数配列を `src/app/api/nippo-check/route.ts` に置き、`session.user.id` が含まれていれば `missingDates: []` を即返す
- 理由:
  - Aだと無駄なDB照会（shiftRows + salesRecords）が毎回走り、将来このAPIを別画面から使ったときに漏れる
  - Cが本来は理想（コード変更なしで運用側が管理できる）だが、対象が現状1人のためシート列追加・列マッピング更新のコストに見合わない。**対象者が増えてきたらCへ移行する**
- 補足: 判定は `session.user.name`（氏名）ではなく `session.user.id`（ログインID）で行う。氏名は同姓の衝突や表記ゆれがあり、既存の照合ロジックも `startsWith` による曖昧一致をしているため、一意なIDの方が安全

---

## 2026-08-27: 育成アプリとのSSO連携（氏名の表記変換とリンク方式）
- 背景: 上司運用の育成アプリ（Cloud Run: `mobile-ikusei-887493910809.asia-northeast1.run.app`）に、Sales Studioログイン済みスタッフが合言葉なしで入れるようにする。仕様は共通シークレットによるHMAC-SHA256署名付きURL（`name` / `ts` / `sig`、有効期限5分）
- **氏名の表記が一致しない問題**（着手前の調査で判明）:
  - 育成アプリが照合する「アルバイト査定」シートB列 = `木下 裕太`（姓名の間に**半角スペース** U+0020）
  - Sales Studioの氏名（スタッフ情報T列） = `木下裕太`（**スペースなし**）
  - → `session.user.name` をそのまま署名すると**26名全員が不一致**になる
- 選択肢:
  - A: 上司に依頼して育成アプリ側で空白を無視した照合にしてもらう
  - B: Sales Studio側で査定シートを読み、正式表記に変換してから署名する
  - C: スタッフ情報シートに「育成アプリ用氏名」列を追加する
- 決定: **B**。「アルバイト査定」シートは Sales Studio が既に読んでいる `GOOGLE_SPREADSHEET_ID`（【Sales Studio用】日報合算）の中にあり、追加の権限も上司側の改修も不要
- 実装:
  - `src/lib/ikusei.ts`: `resolveIkuseiName()` が査定シートB列（データは行4から）を5分キャッシュで読み、**空白（半角・全角）を除去して一致**した行の**元の表記**を返す。`buildIkuseiSsoUrl()` がHMAC-SHA256で署名URLを生成
  - `src/app/api/ikusei-sso/route.ts`: 認証確認 → 氏名解決 → 署名URLへ307リダイレクト。`Cache-Control: no-store` を付与（署名は5分で失効するため、キャッシュされた302を再利用されると期限切れになる）
  - `src/app/api/ikusei-sso/available/route.ts`: ボタンを表示してよいかを返す。社員以上には査定シート登録者の氏名一覧（空白除去済み）も返す
  - `src/components/AnalyticsView.tsx`: 「実績・分析」タブの内部タブ行に `<a href="/api/ikusei-sso?staff=..." target="_blank">` を配置
  - `src/components/AttendanceTable.tsx`: `onStaffChange` で「スタッフ選択」の選択中スタッフを親に通知
- リンク方式の理由: fetchしてJSONでURLを受け取る方式も検討したが、単純なアンカー＋サーバー側リダイレクトなら**ポップアップブロックの影響を受けず**、「押される直前に毎回生成」の要件も自然に満たせる
- **リンク先の対象スタッフ（翔の指示）**: 裏メニューの「スタッフ切り替え」ではなく、**「個人実績」タブ内の『スタッフ選択』ドロップダウンで選んでいる人**を開く。当初は切り替え機能（`impersonated`）ベースで実装したが、社員が日常的に使うのはドロップダウンのほうなので変更した
  - 選択状態は `AttendanceTable` の内部stateだったため、`onStaffChange` コールバックで `AnalyticsView` に持ち上げた
- ボタンの表示条件:
  - アルバイト・業務委託 → 自分が査定シートに載っている場合のみ表示
  - 社員・幹部・管理者 → **選択中のスタッフが査定シートに載っている場合のみ表示**。載っていない人（社員や未登録アルバイト）を選ぶとボタンが消える。押しても合言葉画面しか出ない空振りを防ぐため
- 権限判定: リンクには `?staff=<氏名>` を付けるが、**判定は必ずサーバー側で `session.user.role` を見て行う**。クエリはユーザーが自由に書き換えられるため、画面側の制御だけでは他人のページを開けてしまう
- フォールバック: 査定シートに氏名がない場合・シークレット未設定・エラー時は、仕様どおり育成アプリの `/login`（合言葉入力）へ飛ばす
- 検証（2026-08-27時点の実データ）:
  - 氏名変換: 有効なアルバイト30名中**26名が変換成功**。査定シート側26名は全員マッチし取りこぼしゼロ。未登録は横山雄哉・杉浦大斗・阿部遙人・島田紗和の4名で、**上司に査定シートへの追加を依頼が必要**（追加されれば自動でボタンが出る）
  - 署名: 本番の育成アプリに対して実際にリクエストして確認。正しい署名は受理（`ikusei_auth`・`ikusei_staff_name` cookieが発行される）、**署名の改ざん・氏名だけ差し替えたなりすまし・10分前のts はすべて `/login` へ弾かれた**
- 環境変数: `IKUSEI_SSO_SECRET`（育成アプリ側の `SSO_SHARED_SECRET` と同じ値）。未設定でもアプリは落ちず、合言葉画面へフォールバックする
- 残課題: 氏名の完全一致で本人特定しているため、**同姓同名・氏名変更に弱い**。上司の仕様書にも「将来的にID方式への切り替えを検討」と記載あり

---

## 2026-08-28: 遅刻/早退タブの実装（Talknote 勤怠報告ノート連携）
- 背景: Talknoteに「★関東社員★勤怠報告★」ノートを新設し、当欠・遅刻・早退の報告を集約する運用を開始（給与計算にも使う）。Sales Studio側の裏メニュー「遅刻/早退」は空実装だったため中身を作った
- **前提として判明していたこと**: GASのTalknote取り込みは「実績報告」「☆関東社員☆」の2ノートしか振り分け対象にしておらず、それ以外のノート投稿は `message.markRead()` して破棄していた。そのため勤怠報告の投稿は1件も記録されていなかった（Gmailには残っているので後から取り込める）
- 判断1: **ノートの判定を記号（★/☆）ではなく「勤怠報告」の語で行う**
  - 既存の終了報告は `☆関東社員☆`（白星 U+2606）、新ノートは `★関東社員★`（黒星 U+2605）で文字が違うため衝突はしない。しかし記号に依存するとノート名の装飾を変えただけで壊れる
  - `config.gs` に `KINTAI_NOTE_KEYWORDS = ['勤怠報告']` を配列で持たせた。九州版のノートができたら、そのノート名に含まれる語を足すだけで対応できる
- 判断2: **集計対象は送信者ではなく本文の「スタッフ：」欄**
  - 投稿するのは対象現場のディレクター社員（社員不在時は連絡を受けた社員）なので、送信者名で数えると「報告した社員の遅刻回数」になってしまう。送信者は `reporter` として別に保持し、詳細を開いたときだけ表示する
- 判断3: **年のない日付を受信日時から補完する**
  - 投稿の日付は「8月27日(木)」形式で年がない。受信日時の年を使うが、投稿月と受信月が6か月以上離れている場合は前年/翌年に補正する（12月末の早退を1月に報告するケースで1年ずれるのを防ぐ）
- 判断4: **「勤怠：」の行がない投稿は取り込まない**（翔の指示）
  - 当初は「パースできない投稿も捨てずに その他 として出す」方針にしたが、実データを見て変更した
  - テスト投稿で判明したこと: 1つの投稿に「※西山くん / イオン南砂から急遽テラスモール松戸に移動 / 勤務時間：… / 休憩：…」と**別スタッフの補足が続く**書き方がある。このブロックには「勤怠：」がなく遅刻か早退か判断できない（そもそも勤怠ではなく現場移動の連絡）
  - そのため `勤怠：` が空または無い投稿は `null` を返して一覧から除外する。区分が書かれていれば（当欠/遅刻/早退以外の値でも）「その他」として残す
  - 補足ブロックの内容は、親の投稿を開けば `raw`（全文）で読めるので情報は失われない
  - **1投稿＝1人**として扱う。複数人を1投稿にまとめると2人目以降は記録されないため、運用側で分けて投稿してもらう必要がある
- 実装:
  - `gas/sync-nippo/config.gs`: `SHEET_KINTAI = '勤怠報告受信録'`、`KINTAI_NOTE_KEYWORDS`
  - `gas/sync-nippo/talknote.gs`: `fetchAndSyncTalknote()` に `isKintai` の振り分けを追加。過去メール取込用に `fetchKintaiEmails_PastData()` を追加（受信日時＋送信者で重複チェックあり）
  - `src/app/api/kintai/route.ts`: 受信録を読んでパースし、`?month=YYYY-MM` で絞って日付降順で返す。**社員・幹部・管理者のみ**（裏メニューの機能のため `session.user.role` で判定）
  - `src/components/TardinessView.tsx`: 件数サマリ＋明細テーブル（日付/スタッフ/区分/現場/理由）。行タップで投稿全文・勤務時間・休憩・報告者を展開
- 検証: パーサを記入例・半角コロン・年またぎ・フォーマット無視の4パターンで確認。未認証の `/api/kintai` が401を返すことを確認
- 残課題: 画面の形（明細一覧のまま／スタッフ別集計を足すか）、タブ名を「勤怠」に変えるか、九州版ノートの有無は未確定。給与計算向けのコピー・CSV出力は未実装
